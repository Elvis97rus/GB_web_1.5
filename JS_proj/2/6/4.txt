пока не сильно понял почему, но предполагаю, что как вы говорили это связано с контекстом, т.е. вызывая
closeImageElement.addEventListener('click', this.close); мы можем обратиться через this. к элементу у которого 
нет функции close